import numpy as np

# Create array
arr = np.array([1, 2, 3, 4, 5])
print("Array:", arr)

# Basic operations
print("Mean:", np.mean(arr))
print("Square root:", np.sqrt(arr))
print("Random numbers:", np.random.rand(3))  # 3 random values between 0 and 1
